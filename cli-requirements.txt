# Socrates CLI Dependencies
# Install with: pip install -r cli-requirements.txt

# HTTP Client
requests>=2.31.0

# Terminal UI
rich>=13.7.0           # Beautiful terminal output with colors, tables, panels
prompt-toolkit>=3.0.43  # Interactive prompts with history and auto-completion

# Optional but recommended
colorama>=0.4.6        # Cross-platform colored terminal text (Windows support)
